name: Ping Azure and React App

on:
  schedule:
    - cron: "*/2 * * * *" # Mỗi 2 phút
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend (Azure App Service)
        run: curl -fsS https://medicalbookingcare-bfgwepd5gmd3c5ga.koreacentral-01.azurewebsites.net -o /dev/null

      - name: Ping frontend (React App)
        run: curl -fsS https://bookingcare.hungproject.app -o /dev/null